# assets/env

这里放**预过滤后的 Babylon 环境贴图**（`.env`）。

推荐流程：

1. 打开 `BBL/ENV_BAKE.HTML`
2. 选择 `assets/exr/AboveClouds_08_6K.exr`
3. 生成并下载 `AboveClouds_08_256_webp.env`
4. 把下载的 `.env` 放到本目录
5. 刷新 `BBL/GAME.HTML`

默认会优先加载：`assets/env/AboveClouds_08_256_webp.env`
